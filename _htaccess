# Mise en place de la ré-écriture
RewriteEngine On

# Adresse de base de réécriture
RewriteBase /~roya/codeIgniter

# Règles
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php/$1 [L]
